$(document).ready(function() {
  $('#header').owlCarousel({
    navigation : false,
    slideSpeed : 300,
    paginationSpeed : 400,
    singleItem : true,
    autoPlay : true,
  });
  if ($(window).width() < 960) {
  
  }
  else {
  
  }
});
